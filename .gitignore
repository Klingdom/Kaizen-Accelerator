# Secrets — never commit
.env
.env.local
.env.production
.env.*.local

# Node (not used in MVP but likely later)
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Docker / local build artifacts
.docker-cache/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# Claude Code / agent runtime state
.claude/settings.local.json

# Caddy data (when binding to host instead of named volume)
caddy_data/
caddy_config/

# Future Postgres dump artifacts
*.sql.gz
*.dump
